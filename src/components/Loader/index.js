import React from "react";
import { LoaderWrapper, Loader } from "components/Loader/index.styled";

export default function() {
  return (
    <LoaderWrapper>
      <Loader />
    </LoaderWrapper>
  );
}
